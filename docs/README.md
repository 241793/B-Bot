# B-BOT 官网

纯静态 HTML / CSS / JavaScript，不需要后端、端口或构建工具。将本目录内容作为 GitHub Pages 发布源即可运行。

所有 CSS、JavaScript、JSON 和图片均使用相对路径，支持部署在 GitHub Pages 项目子目录。

本地预览仅用于开发检查：

```bash
python -m http.server 4173
```
