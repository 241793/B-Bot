# 🧩 B-Bot Python 插件开发指南

> 本文档详细介绍 B-Bot 框架支持的 **三种 Python 插件编写方式**，从最轻量到最完整，按你的功能复杂度自由选择。

---

## 📑 目录

- [方式一：单文件函数式（轻量 · 新手首选）](#方式一单文件函数式轻量--新手首选)
- [方式二：类继承式（有状态 · 推荐常规插件）](#方式二类继承式有状态--推荐常规插件)
- [方式三：Package 目录式（复杂插件 · 生产向）](#方式三package-目录式复杂插件--生产向)
- [三种方式对比速查表](#三种方式对比速查表)
- [通用约定与注意事项](#通用约定与注意事项)
- [插件调试技巧](#插件调试技巧)

---

## 方式一：单文件函数式（轻量 · 新手首选）

### 🎯 适用场景

- 快速测试一条命令 / 一个关键词回复
- 功能单一、无状态、不需要生命周期管理
- 临时脚本、Demo、学习框架机制

### 📁 文件位置

```
plugins/
└── hello.py          ← 直接放在 plugins/ 根目录下
```

### 📝 完整示例

```python
"""
hello.py — 最简插件示例
收到"你好" → 回复"你也好 👋"
"""

import re


async def handle_message(msg, middleware):
    """
    消息处理器（异步函数）

    参数:
        msg (dict): 消息对象，包含 content / sender / channel 等字段
        middleware: 框架中间件实例，可调用其方法做回复/广播

    返回:
        dict | None: 返回 {"content": "..."} 即回复；返回 None 表示不处理
    """
    content = msg.get("content", "").strip()

    if content == "你好":
        return {"content": "你也好 👋"}

    # 不匹配时返回 None，框架会继续交给下一个插件处理
    return None


def register(middleware):
    """
    框架加载插件时自动调用此函数。

    在这里把消息处理器注册到 middleware 上即可。
    """
    middleware.register_message_handler(handle_message)
    print("[hello] 插件已加载 ✅")
```

### ⚙️ 工作原理

```
用户发消息 → middleware 分发 → 遍历所有插件的 handler
                                  ↓
                          hello.handle_message()
                                  ↓
                          return {"content": "..."} → 回复用户
```

### ✅ 优点

- **零样板代码**：不需要类、不需要继承、不需要 `__init__.py`
- **保存即热重载**：修改后框架自动 reload，无需重启
- **上手门槛最低**：会写 Python 函数就能写插件

### ⚠️ 局限

- 无状态——每次调用都是全新执行，无法保存计数器、连接池等
- 多个 handler 的优先级需要自己管理
- 不适合功能复杂的插件

---

## 方式二：类继承式（有状态 · 推荐常规插件）

### 🎯 适用场景

- 需要维护状态（计数器、缓存、数据库连接）
- 逻辑较多，需要拆分成多个方法
- 需要利用插件生命周期（`__init__` / 销毁钩子）
- 常规群管、AI 接管、定时任务等

### 📁 文件位置

```
plugins/
└── p_counter/
    ├── __init__.py    ← 可为空，或写 from .main import CounterPlugin
    └── main.py        ← 核心逻辑
```

### 📝 完整示例

**`plugins/p_counter/main.py`**

```python
"""
p_counter — 类继承式插件示例
统计每个群的消息数量，超过阈值时提醒
"""

import logging
from plugin_base import Plugin

logger = logging.getLogger("p_counter")


class CounterPlugin(Plugin):
    """
    继承 Plugin 基类，实现 on_message 方法即可。
    类名建议与插件目录名保持一致，方便面板识别。
    """

    # 类属性：所有实例共享的配置
    THRESHOLD = 10

    def __init__(self, bot):
        """
        构造函数：插件被加载时调用一次。

        参数:
            bot: Bot 实例，可调用 bot.send_message() / bot.call_agent() 等方法
        """
        self.bot = bot
        self.counter = {}       # 群消息计数 {group_id: count}
        self._initialized = True
        logger.info("[CounterPlugin] 初始化完成")

    async def on_message(self, message):
        """
        核心消息处理方法（必须实现）。

        参数:
            message (dict): 消息对象

        返回:
            dict | None: 同方式一的 handler 约定
        """
        group_id = message.get("group_id")
        if not group_id:
            return None  # 私聊消息不处理

        # 计数 +1
        self.counter[group_id] = self.counter.get(group_id, 0) + 1
        count = self.counter[group_id]

        logger.debug("群 %s 消息数: %d", group_id, count)

        # 达到阈值时提醒
        if count == self.THRESHOLD:
            return {
                "content": f"📊 本群已累计 {count} 条消息！"
            }

        return None

    async def on_destroy(self):
        """
        可选：插件被卸载/停用时调用，做清理工作。
        """
        logger.info("[CounterPlugin] 正在清理，最终计数: %s", self.counter)
        self.counter.clear()
```

**`plugins/p_counter/__init__.py`**

```python
# 方式 A：暴露类（推荐）
from .main import CounterPlugin

# 框架检测到 CounterPlugin 继承自 Plugin，会自动实例化
```

### ⚙️ 生命周期

```
插件启用
   ↓
__init__(bot)          ← 一次性初始化
   ↓
on_message(message)    ← 每条消息触发（异步）
   ↓
on_message(message)
   ↓
...
   ↓
插件停用 / 热重载
   ↓
on_destroy()           ← 清理资源（可选实现）
```

### ✅ 优点

- **有状态**：`self.xxx` 可保存任意运行时数据
- **多方法拆分**：逻辑清晰，易于维护
- **生命周期可控**：`__init__` 建连接，`on_destroy` 释放资源
- 面板启停 = 实例化 / 销毁，所见即所得

### ⚠️ 注意

- 类名必须与插件标识一致，否则面板可能识别异常
- `on_message` 必须是 `async` 函数
- 不要在 `on_message` 里做阻塞操作（耗时 IO 用 `asyncio` 或丢到线程池）

---

## 方式三：Package 目录式（复杂插件 · 生产向）

### 🎯 适用场景

- 多文件、多模块协作（工具函数、配置管理、API 封装分离）
- 需要自带 `config.yaml` 配置文件，面板可读写
- 调用内置青龙脚本、本地图床、支付模块等框架能力
- 准备贡献到主仓库，需要规范的目录结构

### 📁 推荐目录结构

```
plugins/
└── qinglong_helper/
    ├── __init__.py       ← 注册入口（框架从此文件加载插件）
    ├── main.py           ← 核心逻辑（Plugin 子类）
    ├── config.yaml        ← 插件配置（面板可编辑）
    ├── utils.py           ← 工具函数
    ├── api_client.py      ← 外部 API 封装
    └── templates/
        └── report.md      ← 模板文件（可选）
```

### 📝 完整示例

**`plugins/qinglong_helper/config.yaml`**

```yaml
# 插件配置 —— 面板修改后会热更新
ql_host: "http://127.0.0.1:5700"
ql_token: ""
notify_threshold: 5
enabled_groups:
  - "group_001"
  - "group_002"
```

**`plugins/qinglong_helper/utils.py`**

```python
"""
工具函数：配置加载、格式化等
"""

import yaml
import os
from pathlib import Path

PLUGIN_DIR = Path(__file__).parent


def load_config():
    """加载同目录下的 config.yaml"""
    config_path = PLUGIN_DIR / "config.yaml"
    if not config_path.exists():
        return {}
    with open(config_path, "r", encoding="utf-8") as f:
        return yaml.safe_load(f)


def format_report(data: dict) -> str:
    """将青龙任务数据格式化为可读文本"""
    lines = ["📋 **青龙任务报告**", ""]
    for name, status in data.items():
        icon = "✅" if status == "success" else "❌"
        lines.append(f"  {icon} {name}: {status}")
    return "\n".join(lines)
```

**`plugins/qinglong_helper/main.py`**

```python
"""
qinglong_helper — Package 式插件核心逻辑
调用内置青龙，查询任务状态并报告
"""

import logging
import asyncio
from plugin_base import Plugin

from .utils import load_config, format_report

logger = logging.getLogger("qinglong_helper")


class QinglongHelperPlugin(Plugin):

    def __init__(self, bot):
        self.bot = bot
        self.config = load_config()
        logger.info(
            "[QinglongHelper] 已加载，QL 地址: %s",
            self.config.get("ql_host", "未配置"),
        )

    async def on_message(self, message):
        content = message.get("content", "").strip()

        # 触发命令：/ql status
        if content == "/ql status":
            return await self._report_qinglong(message)

        # 触发命令：/ql reload
        if content == "/ql reload":
            self.config = load_config()
            return {"content": "🔄 配置已重新加载"}

        return None

    async def _report_qinglong(self, message):
        """调用框架内置青龙能力，获取任务状态"""
        try:
            # 通过 bot 调用框架内置的青龙模块
            tasks = await self.bot.call_qinglong(
                "task_list",
                host=self.config.get("ql_host"),
                token=self.config.get("ql_token"),
            )

            # 格式化输出
            status_map = {t["name"]: t["status"] for t in tasks}
            report = format_report(status_map)

            return {"content": report}

        except Exception as e:
            logger.exception("青龙调用失败")
            return {"content": f"⚠️ 青龙调用失败: {e}"}

    async def on_destroy(self):
        logger.info("[QinglongHelper] 插件已卸载")
```

**`plugins/qinglong_helper/__init__.py`**

```python
"""
入口文件：框架加载插件时从此文件导入。

两种方式任选其一，不要混用：
"""

# ===== 方式 A：暴露类（推荐）=====
from .main import QinglongHelperPlugin

# 框架检测到该类继承自 Plugin，会自动实例化并管理生命周期


# ===== 方式 B：兼容函数式入口 =====
# from .main import QinglongHelperPlugin
#
# def register(middleware):
#     plugin = QinglongHelperPlugin(middleware.bot)
#     middleware.register_plugin(plugin)
```

### ⚙️ 完整调用链

```
用户: /ql status
     ↓
middleware 分发消息
     ↓
__init__.py → 实例化 QinglongHelperPlugin
     ↓
on_message(message) → content == "/ql status"
     ↓
_report_qinglong()
     ↓
self.bot.call_qinglong("task_list", ...)
     ↓
框架内置青龙模块 → HTTP 请求真实青龙面板
     ↓
返回任务列表 → format_report() → 回复用户
```

### ✅ 优点

- **多文件拆分**：逻辑、配置、工具、模板各归其位
- **配置面板化**：`config.yaml` 可在 Web UI 直接编辑
- **完整生命周期**：`__init__` → `on_message` → `on_destroy`
- **可调用框架全部内置能力**：青龙 / 图床 / 支付 / Agent / 知识库
- 适合长期维护、多人协作、贡献主仓库

### ⚠️ 坑点提醒

- `__init__.py` 里用**相对导入**（`from .main import`），不要写绝对导入
- 热重载时子模块路径可能变化，避免在模块顶层做重 IO
- 配置文件修改后记得在插件里 `reload`，或在 `on_message` 里懒加载

---

## 三种方式对比速查表

| 维度 | ① 单文件函数式 | ② 类继承式 | ③ Package 目录式 |
|:---:|:---:|:---:|:---:|
| **代码量** | ⭐ 最少（~20 行） | ⭐⭐ 中等 | ⭐⭐⭐ 较多 |
| **有状态** | ❌ | ✅ `self.xxx` | ✅ `self.xxx` |
| **多文件拆分** | ❌ | 有限 | ✅ 完全支持 |
| **配置文件** | ❌ | 手动读 | ✅ `config.yaml` 面板可读写 |
| **生命周期** | 无 | `__init__` / `on_destroy` | `__init__` / `on_destroy` |
| **调用青龙/图床/支付** | 需手动 import | 需手动 import | ✅ 原生集成 |
| **热重载** | ✅ 完美 | ✅ 支持 | ⚠️ 注意导入路径 |
| **上手难度** | 🟢 极低 | 🟡 低 | 🟠 中等 |
| **适合场景** | 测试/小功能 | 常规插件 | 生产级复杂插件 |

---

## 通用约定与注意事项

### 📌 注册入口（两种方式二选一）

框架加载插件时按以下优先级检测：

```
1. 检测到 Plugin 子类 → 自动实例化（方式②③）
2. 检测到 register(middleware) 函数 → 调用注册（方式①②）
```

> ⚠️ **同一个插件不要混用两套注册方式**，否则 handler 会重复挂载，消息被处理两次。

### 📌 消息对象字段约定

```python
message = {
    "content":    "用户发送的文本内容",
    "sender":     "发送者 ID",
    "sender_name":"发送者昵称",
    "group_id":   "群 ID（私聊为 None）",
    "channel":    "qq / telegram / wechat / ...",
    "msg_id":     "消息唯一 ID",
    "timestamp":  1234567890,
}
```

### 📌 返回值约定

```python
# 回复文本
return {"content": "你好呀"}

# 回复 + 特殊标记
return {"content": "👍", "at_sender": True}

# 不处理，交给下一个插件
return None
```

### 📌 异步规范

- `on_message` / handler 必须是 `async def`
- 耗时操作（HTTP 请求、数据库查询）用 `asyncio` 或 `aiohttp`
- 阻塞代码丢到线程池：`await asyncio.get_event_loop().run_in_executor(None, sync_func)`

---

## 插件调试技巧

### 🔍 1. 日志排查

框架日志默认输出到控制台 + `data/logs/`，插件里用标准 `logging` 即可：

```python
import logging
logger = logging.getLogger("your_plugin_name")
logger.info("普通信息")
logger.debug("调试信息（需开启 DEBUG 级别）")
logger.exception("异常时打印完整堆栈")
```

### 🔍 2. 面板热重载

- 修改插件文件 → 保存 → 面板点击「重新加载插件」
- 无需重启整个 B-Bot 进程
- 热重载失败时会回滚到上一版本，不会崩主进程

### 🔍 3. 本地沙盒测试

```python
# test_plugin.py — 脱离框架快速验证逻辑
import asyncio
from plugins.p_counter.main import CounterPlugin

class FakeBot:
    async def send_message(self, msg):
        print(f"[Bot] → {msg}")

async def main():
    plugin = CounterPlugin(FakeBot())
    for i in range(12):
        result = await plugin.on_message({"group_id": "test", "content": f"msg {i}"})
        if result:
            print(f"[Reply] {result}")

asyncio.run(main())
```

### 🔍 4. 常见报错速查

| 报错 | 原因 | 解决 |
|------|------|------|
| `Plugin class not found` | 类名与目录名不一致 | 保持 `类名 == 目录名` |
| `handler called twice` | 注册方式混用 | 只保留 `register()` 或只暴露类 |
| `ModuleNotFoundError` | 用了绝对导入 | 改为 `from .xxx import` |
| `config.yaml not found` | 路径写死 | 用 `Path(__file__).parent / "config.yaml"` |
| 热重载后状态丢失 | 状态存在模块级变量 | 把状态移到 `self.xxx`（类属性） |

---

## 📚 更多参考

- 框架内置插件源码：`plugins/` 目录下已有插件可作参考
- 框架基类定义：`plugin_base.py`
- 中间件 API：`middleware.py`
- 有问题欢迎提交 Issue / PR 到主仓库 🚀

---

<div align="center">

**Happy Hacking! 🤖**

Made with ❤️ for the B-Bot community

</div>
